defaults:
  - stereo_home_dev
  - _self_

runtime:
  checkpoint_path: ???
  test_csv_path: null
  test_pt_path: null  # if set, use this .pt directory instead of CSV-based loading
  batch_size: 20
  num_workers: 4
  device: auto
  base: false  # set true when evaluating a newMaster (audio-only) checkpoint
